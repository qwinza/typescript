describe('Interface', function () {
    it('should support Interface', function () {
        const seller = {
            id: 1,
            name: "peje"
        };
        console.info(seller);
    });
});
export {};
