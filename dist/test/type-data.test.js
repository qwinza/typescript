"use strict";
describe('Data Type', function () {
    it('should must declare', function () {
        let name = "Faris Ahmad Shidiq";
        let balance = 12000;
        let isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
